import 'package:flutter/material.dart';

class UserTowServices extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'towservices',
        style: TextStyle(fontSize: 50),
      ),
    );
  }
}
